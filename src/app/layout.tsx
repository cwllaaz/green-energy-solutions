import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Footer } from "@/components/footer";
import { Header } from "@/components/header";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL("https://green-energy-solutions.example"),
  title: {
    default: "Green Energy Solutions | Commercial Solar & Energy Engineering",
    template: "%s | Green Energy Solutions",
  },
  description:
    "A professional corporate website demo for a renewable energy company offering solar design, energy engineering, maintenance, and sustainability consulting.",
  openGraph: {
    title: "Green Energy Solutions",
    description:
      "Commercial solar, energy engineering, and maintenance services for sustainable businesses.",
    type: "website",
    images: ["/images/solar-field.jpg"],
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col overflow-x-hidden">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
